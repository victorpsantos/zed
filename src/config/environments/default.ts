export const config = {
  db: {
    // entities: [`${process.env.PWD}/dist/**/*.entity.{js}`],
    // migrations: [`${process.env.PWD}/**/migration/**/*.{js,ts}`],
    // subscribers: [`${process.env.PWD}/**/subscriber/**/*.{js,ts}`],
  },
};
